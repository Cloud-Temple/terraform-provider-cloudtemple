# Per-package statement-coverage floor (percent) for the pure packages.
# Raised as coverage improves (scripts/coverage_ratchet.sh --update);
# CI fails if coverage drops below these values. Never lower by hand.
#
# v1.8.0 re-baseline: removing the VPC provider surface changed the statement mix
# of internal/provider and internal/provider/helpers, so their floors below were
# regenerated with --update to the measured post-removal coverage (verified equal
# to the current value) — honest measurements, not hand-lowered floors. The CI
# gate ignores comment lines; a future --update regenerates the header and drops
# this note.
internal/client 24.8
internal/provider 12.4
internal/provider/helpers 66.7
