---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cloudtemple_public_cloud_vm_snapshot Resource - terraform-provider-cloudtemple"
subcategory: "Public Cloud VM Instances"
description: |-
  Manages a snapshot of a Public Cloud VM instance. Create and delete are asynchronous. The snapshot has no mutable attribute (name and virtualmachineid are immutable), and reverting a VM to a snapshot is not exposed by this provider.
  To manage this resource you will need the following roles:
    - public_cloud_vm_instances_management
    - public_cloud_vm_instances_read
    - activity_read
---

# cloudtemple_public_cloud_vm_snapshot (Resource)

Manages a snapshot of a Public Cloud VM instance. Create and delete are asynchronous. The snapshot has no mutable attribute (name and virtual_machine_id are immutable), and reverting a VM to a snapshot is not exposed by this provider.

To manage this resource you will need the following roles:
  - `public_cloud_vm_instances_management`
  - `public_cloud_vm_instances_read`
  - `activity_read`

## Example Usage

```terraform
variable "virtual_machine_id" {
  type        = string
  description = "The ID of the VM to snapshot."
}

# A point-in-time snapshot taken before a risky change. `name` is immutable —
# renaming recreates the snapshot; reverting a VM to a snapshot is not managed
# by Terraform.
resource "cloudtemple_public_cloud_vm_snapshot" "pre_upgrade" {
  virtual_machine_id = var.virtual_machine_id
  name               = "pre-upgrade"
}

output "snapshot_status" {
  value = cloudtemple_public_cloud_vm_snapshot.pre_upgrade.status
}

output "snapshot_created_at" {
  value = cloudtemple_public_cloud_vm_snapshot.pre_upgrade.created_at
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The name of the snapshot. Immutable (renaming recreates the snapshot).
- `virtual_machine_id` (String) The ID of the VM to snapshot. Immutable.

### Optional

- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `created_at` (String) The creation date of the snapshot (RFC3339).
- `id` (String) The ID of this resource.
- `status` (String) The status of the snapshot (e.g. `available`).

<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String)
- `delete` (String)

## Import

Import is supported using the following syntax:

```shell
# A snapshot is imported by the composite id "<virtual_machine_id>/<snapshot_id>".
terraform import cloudtemple_public_cloud_vm_snapshot.backup 00000000-0000-0000-0000-000000000000/11111111-1111-1111-1111-111111111111
```
