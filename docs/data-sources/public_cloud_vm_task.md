---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cloudtemple_public_cloud_vm_task Data Source - terraform-provider-cloudtemple"
subcategory: "Public Cloud VM Instances"
description: |-
  Used to retrieve a single Public Cloud VM Instances task by id. Tasks are a DIAGNOSTIC object (upstream machine-manager); they are unrelated to the activities that track writes and must never be used to follow a write.
  To query this datasource you will need the public_cloud_vm_instances_read role.
---

# cloudtemple_public_cloud_vm_task (Data Source)

Used to retrieve a single Public Cloud VM Instances task by `id`. Tasks are a DIAGNOSTIC object (upstream machine-manager); they are unrelated to the activities that track writes and must never be used to follow a write.

To query this datasource you will need the `public_cloud_vm_instances_read` role.

## Example Usage

```terraform
# Retrieve a diagnostic task by id.
# NOTE: tasks are diagnostic only and are unrelated to the activities that track
# writes; never use a task to follow a resource create/update/delete.
data "cloudtemple_public_cloud_vm_task" "t" {
  id = "441e1d65-6639-497b-b41b-540955b83a03"
}

output "task_status" {
  value = data.cloudtemple_public_cloud_vm_task.t.status
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) The ID of the task to retrieve. (The live API returns UUID task ids; the underlying schema documents an opaque string.)

### Read-Only

- `completed_at` (String) The completion date of the task (RFC3339), if completed.
- `created_at` (String) The creation date of the task (RFC3339).
- `failure_code` (String) A stable failure code when the task failed (e.g. InsufficientStorageCapacity).
- `message` (String) A human-readable message describing the task outcome.
- `retried_from_id` (String) The ID of the task this task was retried from, if any.
- `status` (String) The status of the task (pending, running, completed, failed, cancelled...).
- `task_type` (String) The type of the task.
- `updated_at` (String) The last update date of the task (RFC3339).
- `vm_id` (String) The ID of the VM the task relates to (may be empty).


