---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cloudtemple_public_cloud_vm_backup_policy Data Source - terraform-provider-cloudtemple"
subcategory: "Public Cloud VM Instances"
description: |-
  Used to retrieve a single Public Cloud VM Instances backup policy, by id or by name. A backup policy is required to create a VM (backup_policy_id). The API has no by-id endpoint; selection is done by listing and matching.
  To query this datasource you will need the public_cloud_vm_instances_read role.
---

# cloudtemple_public_cloud_vm_backup_policy (Data Source)

Used to retrieve a single Public Cloud VM Instances backup policy, by `id` or by `name`. A backup policy is required to create a VM (`backup_policy_id`). The API has no by-id endpoint; selection is done by listing and matching.

To query this datasource you will need the `public_cloud_vm_instances_read` role.

## Example Usage

```terraform
# Retrieve a Public Cloud VM Instances backup policy by name.
# A backup policy is required to create a VM (backup_policy_id).
data "cloudtemple_public_cloud_vm_backup_policy" "daily" {
  name = "Daily backup — 30 days"
}

output "daily_retention" {
  value = data.cloudtemple_public_cloud_vm_backup_policy.daily.retention
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (String) The ID of the backup policy to retrieve. Conflicts with `name`.
- `name` (String) The name of the backup policy to retrieve. Conflicts with `id`.

### Read-Only

- `description` (String) The human-readable description of the backup policy.
- `retention` (Number) The number of restore points retained by the policy.
- `schedule_cron` (String) The cron expression driving the backup schedule.
- `schedule_window_duration_minutes` (Number) The duration (minutes) of the backup window.
- `schedule_window_start_hour` (Number) The hour of day (0-23) at which the backup window starts.


