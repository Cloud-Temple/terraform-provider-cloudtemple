---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cloudtemple_public_cloud_vm_flavor Data Source - terraform-provider-cloudtemple"
subcategory: "Public Cloud VM Instances"
description: |-
  Used to retrieve a single Public Cloud VM Instances flavor (a predefined vCPU/RAM sizing pair), by id or by name. The API has no by-id endpoint for flavors; selection is done by listing and matching.
  To query this datasource you will need the public_cloud_vm_instances_read role.
---

# cloudtemple_public_cloud_vm_flavor (Data Source)

Used to retrieve a single Public Cloud VM Instances flavor (a predefined vCPU/RAM sizing pair), by `id` or by `name`. The API has no by-id endpoint for flavors; selection is done by listing and matching.

To query this datasource you will need the `public_cloud_vm_instances_read` role.

## Example Usage

```terraform
# Retrieve a flavor (a predefined vCPU/RAM sizing pair) by name.
data "cloudtemple_public_cloud_vm_flavor" "medium" {
  name = "m2.medium"
}

output "flavor_sizing" {
  value = {
    vcpu   = data.cloudtemple_public_cloud_vm_flavor.medium.vcpu
    ram_gb = data.cloudtemple_public_cloud_vm_flavor.medium.ram_gb
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (String) The ID of the flavor to retrieve. Conflicts with `name`.
- `instance_family_id` (String) The ID of the instance family, used to narrow the search. Populated with the flavor's family on read.
- `name` (String) The name of the flavor to retrieve (e.g. `dev-micro`). Conflicts with `id`.

### Read-Only

- `ram_gb` (Number) The amount of RAM of the flavor, in GB.
- `vcpu` (Number) The number of vCPUs of the flavor.


